services:
  grand-river-commons:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grand-river-commons
    ports:
      - "5173:5173"
    environment:
      HOST: 0.0.0.0
      PORT: 5173
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
